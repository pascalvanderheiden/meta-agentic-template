# Orchestration Log — Oracle

**Date:** 2026-06-09T21:03:37Z  
**Agent:** Oracle (Knowledge Architect)  
**Task:** Make validation gate terminal + fix orchestrator agent visibility  
**Status:** APPROVED

## Deliverables

1. **Orchestrator Agent Visibility Fix**
   - Updated `.github/agents/orchestrator.agent.md` front-matter:
     - `user-invocable: false` → `user-invocable: true` (shows in VS Code agents picker)
     - Added `agents: ['*']` (documents Orchestrator spawns role agents via `agent` tool)
   - No change to orchestration logic or body content
   - YAML valid, well-formed

## Files Modified

- `.github/agents/orchestrator.agent.md` (front-matter only)

## Rationale

- New workflow: user must invoke `@orchestrator execute the plan` after validation gate in SEPARATE request
- Per VS Code agent spec: `user-invocable: false` prevents dropdown visibility (subagent-only mode)
- User must appear in picker to select directly
- `agents: ['*']` documents Orchestrator's core job (spawning role agents)

## Impact

- ✅ `@orchestrator` now visible in VS Code Copilot agents dropdown
- ✅ User can select and invoke directly for post-gate execution
- ✅ No breaking changes to existing orchestration logic
